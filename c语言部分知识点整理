1.设ｃ语言中int 类型数据占4个字节则short 类型数据占2字节

2.输出为10进制输出左对齐n个字符m位精度%-n.mf

-： 左对齐

n： 最小字段宽度

.m： 精确度保留小数4位

f： double精度浮点数

e： 科学计数法

3.

    #define ADD (x) x+x
    s = ADD(n+m)*k
    会替换成n+m+n+m*k

4.实型常数:E(e)前面为十进制整数或实数且不能省略，E(e)后面为十进制整数且   不能为实数且不能省略

5.在C语言中整型常量分为十进制整型常量、八进制整型常量和十六进制整型常量三种表示形式

6.在一个函数内的复合语句中定义的变量是局部变量其作用范围是从定义处到复合语句的结束，也就是只在本复合语句内有效

7.转义符以反斜杠“\”开头

8.c语言中main函数名称只能为main并且有且仅有一个

9.main 函数中三个默认参数按照顺序描述：参数数量，具体参数，环境变量

10.一个 C 程序的执行先从具有 attribute((constructor)) 标记的函数开始，若没有，则先从 main 函数开始

11.switch语句能够改写为if语句

12.一个 C 语言程序是由函数组成

13.括号中逗号运算符返回值是最后的表达式的值

14.switch语句中没有break会执行满足条件的和之后的所有条件

15.①标识符的第一个字符必须是字母或下划线，后面的字符可以是字母、数字或下划线，不能为其它字符②标识符的长度可以是一个或多个字符，最长不允许超过32个字符③标识符不能和C语言的关键字相同，也不能和用户自定义的函数或C语言库函数同名

16.简单变量作为实参时，将把该变量所占内存单元的值传递给形参，实参和形参各占不同的内存单元，传递完后，实参和形参不再有任何联系，所以这种传递方式也叫做单向值传递方式

17.静态局部变量只对定义它的函数体始终可见，函数体执行完过后虽然还存在，但是无法被其他的使用了

18.用C语言编写的代码程序是一个源程序

19.行优先和列优先（假设数组为Ａ［ｎ］［ｍ］，数组下标从0开始）　

　①行优先：基址＋（行数）*　ｍ　＋　列数）×　每个元素所占内存单位

　②列优先：基址＋（列数　*　ｍ　＋　行数）×　每个元素所占内存单位
